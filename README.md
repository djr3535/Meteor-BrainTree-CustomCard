# Meteor-BrainTree-CustomCard
Custom fields in Meteor for BrainTree interface.  Prototype code to check functionality requires error checking/handling, comments and additional security around credentials.
